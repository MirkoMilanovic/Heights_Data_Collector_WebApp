# Heights_Data_Collector_WebApp
This Python program makes a PostgreSQL database for storing users' heights and emails. It executes an interactive HTML page with a form for getting the user's data (height and email), it stores it in the DB. The average height of all users is calculated and sent back to the email.
